9c08a21e4: batch size 64, epislon decay rate: 0.9999998 median reward continuously oscillate around three after 26k episodes, even after 300k episodes, progress is still oscillating around 3.

6ca45e32
- batch size is 512
- evaluation progress rises from 0 to around 12 after 2M steps (clicks) (maybe 4 M) and then stays constant after that.
- win rate stablizes around 0.5 after 4M clicks

4d3dc52
(cd05ff8474725c68cebb62cce9c8ede3f2c4e875):
- batch size is 64, eval progress avg rises much slower and tops off at 8 after 36 M steps.

20391b4b
- batch size back to 512
- after about step 11 million or so, changed code so that epsilon is 0.01
- also update target model every 5 trainings
- after that, it went to shit. median progress went down from 15 to 8between 11.5 million step to 13.5 million steps.
- not sure about loss, it seems that i didn't previously log loss so i don't know.
- winrate dropped from 0.55 to 0.22 in the same time
- the theory is that, when epsilon is 0.01, you are doing on-policy learning. the model's training data is mostly good clicks. so it starts forgetting about the bad clicks (on the mine)
- at 13.4 million, start computing td-error (avg and max)
- see that td-error max is 1.3 and td-mean is 0.05.  also mine hit % in replay is 0.08
- changed epsilon_min to 0.1, no longer override epsilon to 0.01, epsilon decay is 0.99975
- so the cond winrate went up from 0.03 to 0.18 as epsilon fell but quickly the conditional winrate capped at 0.2 as epsilon reached 0.1
- restart run with a lower epsilon decay of 0.999975
- unconditional win rate climbed back to 10% but it seems to be slow.
- seems that when epsilon hit 0.1 and stayed constant, median progress and winrate flat-lined. (around 16 million steps). so i'm going to raise epsilon decay to 0.9999975
- start another training at 16.5 million steps
- remember that cond_winrate, progress_med is calculated with exploration
- stopped at 17.5M steps.
- until 20M steps, eval_avg_max_q collapsed to 0
- until 31M steps, the eval_avg_max_q cycled up to 0.35 but collapsed again and cycled again.
- set learning rate to 0.00003
- begin logging gradient
- gradient updates at final layer's kernel weights are 10^-6. final layer's bias is 10^-4.  That's too small.
- set learning rate to 0.0003, also resets Adam momentum after loading model
- loss is very small, 0.003, because most Q values other than the one that we just took action at are correct
- i checked the output of second last layer (DENSE_UNIT number of neurons).  All neurons except for one (neuron 200th (0th based index)) is just outputting 0.  This is post-activation.  So they must be stuck in the negative region of ReLU function.
- found a bug where learning rate is set to 0 by accident. fixed
- but kernel gradient is still extremely small, so kernel weights basically don't move.
- bias weights move slowly
- eval avg max q also doesn't move

e5dad787:
- bias weights are moving significantly
- kernel gradient has slightly higher variance and the weights are still moving slowly but at least moves a bit
- the last layer input are no longer all zeros, there are a significant number of non-zeroes. exactly 212 out of 512 (steps 30000 to 70k)
- by 80k steps, eval progress avg moved around 6, eval_avg_max_q moved to 0.7 and steady, loss went down to 4*10^-4, gradients became pretty narrow
- then i changed epsilon, start at 0.9, decay at 0.99995, min 0.2
- most of the kernel parameters haven't changed except for dense_2, it became bimodal